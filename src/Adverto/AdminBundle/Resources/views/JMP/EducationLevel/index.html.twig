{% extends 'AdminBundle:Base:base.html.twig' %}


{% block title %}Admin - Education Levels{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block content %}
    <!-- Content Section Start -->
    <div class="content-section">
        <div class="container-liquid">
            {% include 'AdminBundle:JMP:Base/sidebar.html.twig' %}   
            <div class="float-row-smaller listingPages">
                <div class="col-xs-12">
                    <div class="col-xs-8"><h1>Admin - Education Levels</h1></div>
                    <div class="col-xs-4">
                        <a href="{{ path('admin_jmp_education_level_new') }}" class="addPageBtn pull-right">CREATE A EDUCATION LEVEL</a>
                    </div>
                </div>
                <div class="col-xs-12 headPages">
                    <div class="col-xs-1">&nbsp;</div>
                    <div class="col-xs-5">Education Level Name</div>
                    <div class="col-xs-6">LAST LOG</div>
                </div>
                <div id="pages_sortable_list" class="ui-sortable">
                    <div id="infoSorting"></div>
                    {% for educationlevel in educationlevels %}
                    <div class="col-xs-12 listing" id="item_{{ educationlevel.position }}">
                        <div class="col-xs-1">&nbsp;</div>
                        <div class="col-xs-5">{{ educationlevel.name }}</div>
                        <div class="col-xs-2">{{ educationlevel.userModified }}</div>
                        <div class="col-xs-2">{{ educationlevel.dateModified|date("m/d/Y") }}</div>
                        <div class="col-xs-2">
                            <a href="#" class="actionBtn drag ui-sortable-handle" rel="">Drag</a>
                            <a href="{{ path('admin_jmp_education_level_edit', {id:educationlevel.id}) }}" class="actionBtn edit">Edit</a>
                        </div>
                    </div>
                    {% endfor %}
                </div>
                <div class="col-xs-12 pagesTip">
                    <div class="col-xs-1">&nbsp;</div>
                    <div class="col-xs-7"><span>Tip.</span> Do you need to add another education level?</div>
                    <div class="col-xs-4"><a href="{{ path('admin_jmp_education_level_new') }}" class="addPageBtn pull-right">CREATE A EDUCATION LEVEL</a></div>
                </div>
            </div>
            <!-- Row End -->
        </div>
    </div>
    <script type="text/javascript">
        var relatedPage = 'jmp/educationlevel-units';
    </script>            <!-- Content Section End -->
{% endblock %}
